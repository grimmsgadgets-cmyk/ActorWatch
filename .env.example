# ActorWatch — environment variable reference
# Copy to .env and fill in values as needed.
# All variables are optional — defaults produce a working standalone install.

# ─── LLM / Ollama ──────────────────────────────────────────────────────────────
# Base URL for the Ollama sidecar.  Matches docker-compose default.
OLLAMA_BASE_URL=http://ollama:11434

# Model to use for notebook synthesis.
OLLAMA_MODEL=llama3.1:8b

# Set to 1 to make LLM synthesis required (fail fast if Ollama is unreachable).
# Leave unset / 0 for graceful degradation to deterministic output.
ENFORCE_OLLAMA_SYNTHESIS=0

# ─── Auto-refresh ──────────────────────────────────────────────────────────────
# Minutes between actor refresh cycles (0 = use built-in default).
PAGE_REFRESH_AUTO_TRIGGER_MINUTES=0

# ─── MITRE seed ────────────────────────────────────────────────────────────────
# Set to 1 to auto-seed catalog entries from MITRE ATT&CK on first run.
MITRE_AUTO_SEED_ACTORS=0

# ─── Branding / embedding (ThreatSpire / custom host) ─────────────────────────
# Application display name shown in the UI.
ACTORWATCH_APP_NAME=ActorWatch

# Sub-path prefix when ActorWatch is mounted inside a host platform.
# Leave empty for standalone operation at /.
# Example for ThreatSpire: /modules/actorwatch
ACTORWATCH_BASE_PATH=

# Primary accent colour (CSS hex).
ACTORWATCH_PRIMARY_COLOR=#35527f

# UI theme: dark (default) or light.
ACTORWATCH_THEME=dark

# Path to a custom logo file served as a static asset.
ACTORWATCH_LOGO_PATH=

# Set to 1 when a host platform supplies its own auth middleware.
# Community edition: leave unset (single-user, no auth).
ACTORWATCH_AUTH_ENABLED=0

# ─── Licensing (Phase 2 — not yet implemented) ─────────────────────────────────
# Tier 1 / Tier 2 license key from Lemon Squeezy.
# Leave unset to run as Community Edition.
# ACTORWATCH_LICENSE_KEY=LS-XXXX-XXXX-XXXX-XXXX

# Link shown in upgrade prompts for locked Tier 1/2 features.
# ACTORWATCH_UPGRADE_URL=

# ─── Security ──────────────────────────────────────────────────────────────────
# Set to 1 when running behind a trusted reverse proxy (nginx, Traefik, etc.)
# so that X-Forwarded-For / X-Forwarded-Proto headers are honoured.
TRUST_PROXY_HEADERS=0
