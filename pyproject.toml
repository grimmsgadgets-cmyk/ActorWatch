[project]
name = "actorwatch"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi",
  "uvicorn[standard]",
  "jinja2",
  "httpx",
  "python-multipart>=0.0.22",
]

[dependency-groups]
dev = [
  "pytest",
]
