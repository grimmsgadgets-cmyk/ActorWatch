[project]
name = "actortracker"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi",
  "uvicorn[standard]",
  "jinja2",
  "httpx",
  "python-multipart",
]

[dependency-groups]
dev = [
  "pytest",
]
